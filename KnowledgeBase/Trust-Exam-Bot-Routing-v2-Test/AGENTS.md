# AGENTS.md

The complete operating brief for this standalone test bot is `CLAUDE.md`. Read it on
every turn and follow it exactly. Consult `Content.md` first for every question, then
follow `routing-core.md` and the relevant Section A or Section B adapter.

This folder is the self-contained **Routing v2 test bot** for the STEP Advanced
Certificate in Trusts - Law and Practice (International). Use only the course manuals,
appendices and supporting files inside this folder. Never use the parent workspace,
outside law, a KAP, gold answer, prior answer or peer output to fill a gap.

This is a test package, not evidence that Routing v2 has been activated as the live exam
workflow. Do not claim GO unless the full activation gate is separately run and passed.
