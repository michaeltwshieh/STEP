Convert all PDFs in a Materials subfolder to Markdown files in the matching Markdown subfolder.

Usage: /convert-pdfs <subfolder>

Run the following bash command from the project root, using the subfolder name provided in the arguments:

```bash
cd "/Users/michaelshieh/Desktop/Claude Projects/STEP Exam" && .venv/bin/python convert_pdfs.py "$ARGUMENTS"
```

After running, report which files were converted and where the output was saved.
